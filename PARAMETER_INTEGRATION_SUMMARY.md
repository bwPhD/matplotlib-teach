# 参数集成总结 - 将百科参数添加到交互式调整界面

## ✅ 已完成的集成

### Tab1: Line2D (线条) - 新增参数

#### 1. **绘制样式 (drawstyle)** ✅
- **位置**：Expander（默认折叠）
- **选项**：从 `get_drawstyle_options()` 动态获取
  - `'default'` - 线性插值（默认）
  - `'steps'` - 阶梯图（等同于 steps-pre）
  - `'steps-pre'` - 在点之前保持水平
  - `'steps-mid'` - 在点中间保持水平
  - `'steps-post'` - 在点之后保持水平
- **效果**：实时预览中，当选择非 default 时，使用较少点数以便看清阶梯效果
- **查看选项按钮**：点击可查看完整的 drawstyle 参数百科

#### 2. **线端样式 (capstyle)** ✅
- **位置**：Expander（默认折叠）
- **选项**：从 `get_capstyle_options()` 动态获取
  - `'butt'` - 平头（默认）
  - `'round'` - 圆头
  - `'projecting'` - 方头
- **智能应用**：仅当 `linewidth >= 5` 时才应用（因为细线看不出效果）
- **查看选项按钮**：点击可查看完整的 capstyle 参数百科

#### 3. **连接样式 (joinstyle)** ✅
- **位置**：Expander（默认折叠）
- **选项**：从 `get_joinstyle_options()` 动态获取
  - `'miter'` - 尖角连接（默认）
  - `'round'` - 圆角连接
  - `'bevel'` - 斜角连接
- **智能应用**：
  - 仅当 `linewidth >= 5` 时才应用
  - 使用折线数据（至少3个点）以便看清连接效果
- **查看选项按钮**：点击可查看完整的 joinstyle 参数百科

### Tab3: Collections (散点图) - 新增参数

#### 4. **填充样式 (fillstyle)** ✅
- **位置**：Expander（默认折叠）
- **选项**：从 `get_fillstyle_options()` 动态获取
  - `'full'` - 完全填充（默认）
  - `'left'` - 左半部分填充
  - `'right'` - 右半部分填充
  - `'bottom'` - 下半部分填充
  - `'top'` - 上半部分填充
  - `'none'` - 不填充（仅边框）
- **智能应用**：仅对可填充的标记有效（如 'o', 's', '^', 'v', 'D', 'd', 'p', 'h', 'H', '8', '*'）
- **查看选项按钮**：点击可查看完整的 fillstyle 参数百科

## 📊 参数组织策略

### 展开/折叠策略
- **常用参数**：默认展开（`expanded=True`）
  - linestyle, linewidth, color, marker
  - marker (scatter), cmap (scatter)
- **高级参数**：默认折叠（`expanded=False`）
  - drawstyle, capstyle, joinstyle
  - fillstyle

### 智能应用逻辑
1. **capstyle 和 joinstyle**：
   - 仅当 `linewidth >= 5` 时应用
   - 因为细线看不出这些效果

2. **joinstyle**：
   - 使用折线数据（至少3个点）
   - 自动切换数据以便看清连接效果

3. **fillstyle**：
   - 仅对可填充的标记应用
   - 自动检测标记类型

4. **drawstyle**：
   - 非 default 时使用较少点数
   - 便于看清阶梯效果

## 💻 代码生成优化

### 动态参数构建
- 根据参数条件动态构建 `plot_kwargs` 或 `scatter_kwargs`
- 只在需要时添加参数，避免冗余代码

### 代码示例
```python
# Line2D 示例
plot_kwargs = {
    'linestyle': line_style,
    'linewidth': line_width,
    'color': color,
    'marker': marker,
    'markersize': 8,
    'drawstyle': drawstyle,
}

# 仅当 linewidth >= 5 时才添加
if line_width >= 5:
    plot_kwargs['solid_capstyle'] = capstyle
    plot_kwargs['solid_joinstyle'] = joinstyle

# Scatter 示例
scatter_kwargs = {
    's': area,
    'c': colors,
    'alpha': alpha_scatter,
    'cmap': cmap_choice,
    'marker': marker_scatter,
}

# 仅对可填充标记应用
if marker_scatter in fillable_markers:
    scatter_kwargs['fillstyle'] = fillstyle_scatter
```

## 🎯 用户体验提升

### 1. 渐进式披露
- 常用参数默认展开，高级参数默认折叠
- 减少视觉混乱，保持界面简洁

### 2. 智能提示
- 使用 `help` 参数提供简短说明
- 参数名称清晰，易于理解

### 3. 实时反馈
- 所有参数变化立即反映在预览图中
- 代码自动更新，显示当前参数值

### 4. 深度探索
- 每个参数都有"查看选项"按钮
- 点击可查看完整的参数百科和所有选项

## 📋 参数列表总结

### Tab1: Line2D
| 参数 | 类型 | 默认展开 | 查看选项 |
|------|------|---------|---------|
| linestyle | 选择框 | ✅ | ✅ |
| linewidth | 滑块 | ✅ | - |
| color | 颜色选择器 | ✅ | ✅ |
| marker | 选择框 | ✅ | ✅ |
| drawstyle | 选择框 | ❌ | ✅ |
| capstyle | 选择框 | ❌ | ✅ |
| joinstyle | 选择框 | ❌ | ✅ |

### Tab3: Scatter
| 参数 | 类型 | 默认展开 | 查看选项 |
|------|------|---------|---------|
| 点数量 | 滑块 | ✅ | - |
| marker | 选择框 | ✅ | ✅ |
| fillstyle | 选择框 | ❌ | ✅ |
| cmap | 选择框 | ✅ | ✅ |
| alpha | 滑块 | ✅ | - |

## 🔧 技术实现

### 动态选项获取
```python
from catalogs.line import get_drawstyle_options, get_capstyle_options, get_joinstyle_options
from catalogs.marker import get_fillstyle_options

drawstyles = get_drawstyle_options()
capstyles = get_capstyle_options()
joinstyles = get_joinstyle_options()
fillstyles = get_fillstyle_options()
```

### 条件应用
```python
# 根据条件应用参数
if line_width >= 5:
    plot_kwargs['solid_capstyle'] = capstyle
    plot_kwargs['solid_joinstyle'] = joinstyle

if marker_scatter in fillable_markers:
    scatter_kwargs['fillstyle'] = fillstyle_scatter
```

## 📝 后续优化建议

1. **添加更多参数**：
   - markersize, markeredgewidth, markeredgecolor
   - alpha, zorder
   - dashes（自定义虚线）

2. **参数分组**：
   - 使用更清晰的视觉分组
   - 添加参数说明卡片

3. **预设模板**：
   - 常用参数组合的快速选择
   - 一键应用样式

---

**完成时间**：2024-12-19
**影响范围**：Tab1 (Line2D) 和 Tab3 (Scatter)
**测试状态**：✅ 通过 lint 检查

